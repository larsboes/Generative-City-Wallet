# Local LLM Preparation Artifacts

Implementation-side artifacts for the mobile-first Gemma plan.

- `DEVICE-MATRIX-GALLERY.md` — flagship-only benchmark matrix template.

Related code artifacts:

- `apps/mobile/src/local-llm/` — TS bridge + tool schema + mapper scaffold.
- `apps/mobile/LOCAL_LLM_SPIKE.md` — native module integration runbook.
- `tests/fixtures/local_llm/` — frozen `IntentVector` + `CompositeContextState`.
- `scripts/dev/smoke_local_llm.py` — fixture validation + parser-rate smoke runner.

